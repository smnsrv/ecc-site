# Скопируйте в `.env` в корне (в git не попадает).
# VITE_TG_* нужны для npm run dev: тест testTelegram() в консоли и CTA — через Vite-прокси.
# В production Telegram шлётся из Google Apps Script (свойства TG_TOKEN / TG_CHAT_ID), не из .env

VITE_TG_TOKEN=
VITE_TG_CHAT_ID=
