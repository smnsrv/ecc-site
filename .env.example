# Скопируйте в файл `.env` в корне проекта (не коммитить ` .env` в публичный репо).
# Для GitHub Pages: задайте те же переменные в Secrets репозитория и в workflow перед `npm run build` экспортируйте VITE_*

VITE_TG_TOKEN=
VITE_TG_CHAT_ID=
